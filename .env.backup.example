# Copy to .env.backup.local (ignored by Git). Do NOT use VITE_ for secrets.
SUPABASE_BACKUP_KEY=
BACKUP_ROOT=
BACKUP_KEY_FILE=
# Directory containing PostgreSQL 17+ pg_dump. Local default is documented.
PG_BIN=
